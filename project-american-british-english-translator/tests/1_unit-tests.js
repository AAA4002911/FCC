const chai = require('chai');
const assert = chai.assert;

const Translator = require('../components/translator.js');
const translator = new Translator();
suite('Unit Tests', () => {


  suite('to British English', () => {

    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })

    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })

    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })

    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })
    test('Translate Mangoes are my favorite fruit', function(done) {
      var input = 'Mangoes are my favorite fruit';
      var output = 'Mangoes are my <span class="highlight">favourite</span> fruit';
      console.log(translator.translate(input, 'american-to-british'))
      assert.equal(translator.translate(input, 'american-to-british'), output)
      done()
    })

  });

});
